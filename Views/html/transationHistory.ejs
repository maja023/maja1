<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.1.3/dist/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <link rel="stylesheet" href='../css/users.css'>
    <title>transation table</title>
</head>
<body>

    <style>
        #ppic:hover{
            width: 100%;
            height: 100%;
         border-radius:0%;
        }

        #ppic{
            width: 60px;height: 60px;border-radius: 100%;
        }
        .usersTablemain {
    font-family: monospace;
}
        
    </style>


    <%-include('../layout/header');%> 

<div class="usersTablemain">
    <table class="table">
        <thead class="thead-dark">
          <tr>
            <th scope="col">Id</th>
            <th scope="col">transation</th>
            <th scope="col">amount</th>
            <th scope="col">status</th>
            <th scope="col">date</th>

          </tr>
        </thead>

        <tbody>
          

<% if(send.length>0){%>
    
    <%  for(let i=0;i<send.length;i++){%>
        <tr>
        <th scope="row"><%= send[i].transationId %></th>
     
    
          
<% if(send[i].senderNumber==send[i].transationId && send[i].transationStatus=="send money" ){%>

          
  <td><%= send[i].reciverNumber %></td>
        
          <% }else{%>

            <td><%= send[i].senderNumber %></td>
            <%}%>


       
        <td><%= send[i].transationAmount %></td>
        <td><%= send[i].transationStatus %></td>
        <td style="color: red;"><%= send[i].transationDate %></td>

    </tr>

        <% }%>

    <% }else{%>
    <p style="color: red;text-align: center;padding:10px 0px">data not Found !</p>
    <% } %>


            
        </tbody>

        
      </table>
</div>
    
</body>
</html>